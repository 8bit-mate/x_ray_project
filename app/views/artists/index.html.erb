<p style="color: green"><%= notice %></p>

<h1>Artists</h1>

<table class="table table-responsive table-striped table-hover">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col"><%= sort_link(@q, :last_name, "Last name") %></th>
      <th scope="col"><%= sort_link(@q, :first_name, "First name") %></th>
      <th scope="col"><%= sort_link(@q, :songs_count, "Songs in the database") %></th>
    </tr>
  </thead>
  <tbody>
    <% @artists.each_with_index do |artist, index| %>
      <tr>
        <th scope="row">
          <%= item_index(index, @pagy) %>
        </th>
        <td>
          <%= link_to artist.last_name, artist %>
        </td>
        <td>
          <%= link_to artist.first_name, artist %>
        </td>
        <td>
          <%= artist.songs_count %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%== pagy_bootstrap_nav(@pagy) %>