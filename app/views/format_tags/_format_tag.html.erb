<div id="<%= dom_id format_tag %>">
  <%= render "shared/tag", tag_obj: format_tag %>

  <%= turbo_frame_tag "records", src: format_tag_records_path(format_tag), loading: :lazy, target: "_top" do %><% end %>
</div>
