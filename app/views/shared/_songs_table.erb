<table class="table table-responsive table-striped table-hover">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col"><%= sort_link(@q, :song_title_title, "Title") %></th>
      <th scope="col">Performed by</th>
      <th scope="col"><%= sort_link(@q, :year_of_release, "Year of release") %></th>
    </tr>
  </thead>
  <tbody>
    <% songs.each_with_index do |song, index| %>
      <tr>
        <th scope="row">
          <%= index + 1 %>
        </th>
        <td>
          <%= link_to song.title, song %> <%= song.print_variation %>
        </td>
        <td>
          <% song.artists.each do |artist| %>
            <%= link_to artist.full_name, artist %>
          <% end %>
        </td>
        <td>
          <%= song.year_of_release %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%== pagy_bootstrap_nav(@pagy) %>