<% tags.each do |tag| %>
  <%= link_to [url_scope, tag], style:"text-decoration:none" do %>
    <span class="<%= tag.span_class_name %>" title="<%= tag.short_description %>"><%= tag.name %></span>
  <% end %>
<% end %>