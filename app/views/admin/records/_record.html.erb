<div id="<%= dom_id record %>">
  <p>
    <strong>Tech info:</strong>
    <%= record.tech_info %>
  </p>

  <p>
    <strong>Category:</strong>
    <% tag = record.category %>
    <%= render "shared/tag_display_admin", tag: tag %>
  </p>

  <p>
    <strong>Format:</strong>
    <% tags = record.format_tags %>
    <%= render "shared/tag_array_display_admin", tags: tags %>
  </p>

</div>
