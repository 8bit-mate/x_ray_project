<p style="color: green"><%= notice %></p>

<h1>Records</h1>

<div id="records">
  <% @records.each do |record| %>
    <%= render record %>
    <p>
      <%= link_to "Show this record", admin_record_path(record) %>
    </p>
  <% end %>
</div>

<%= link_to "New record", new_admin_record_path %>
