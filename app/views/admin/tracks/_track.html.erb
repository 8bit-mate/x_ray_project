<div id="<%= dom_id track %>">
  <p>
    <strong>Number:</strong>
    <%= track.number %>
  </p>

  <p>
    <strong>Song:</strong>
    <%= track.song.human_full_title %>
  </p>

  <% if track.web_audio.attached? %>
    <audio controls>
      <source src="<%= rails_blob_url(track.web_audio) %>" type="audio/mpeg">
      Your browser does not support the audio element.
    </audio>
  <% else %>
    <p> Track has no audio!</p>
  <% end %>

  <p>
    <strong>Record assigned:</strong>
    <% tag = track.record %>
    <% url_scope = :admin %>
    <% if tag %>
      <%= link_to [url_scope, tag], style:"text-decoration:none" do %>
        <span class=""><%= tag.id %></span>
      <% end %>
    <% end %>
  </p>
</div>
